<!DOCTYPE html>
<html lang="en">
  <%- include ('../head_toes/header') %>
  <body>
    <div class="row">
      <div class="col-md-4">
        <h1><%= user.username %></h1>
        <div class="thumbnail">
          <img src="<%= user.profilePic %>" alt="user profile image" />
          <div class="caption">
            <a href="mailto:<%= user.email %>"><%= user.email %></a>
          </div>
        </div>
      </div>
      <div class="col-md-8">
        <h3><%= user.username %>'s posts:</h3>

        <% let count = 0 %>
        <% bizIdeas.forEach((idea)=> { %>
          <% count++ %>
        <% }); %>
        <% if(count != 0){ %>
          <ul class="list-group">
          
          <% bizIdeas.forEach(function(idea) { %>
            <li class="list-group-item"><%= idea.title %></a></li>
          <% }); %>
          </ul>
        <% } else { %>
          <p>you have not made any post yet.</p>
        <% } %>





        
      </div>
    </div>

    <%- include ('../head_toes/footer') %>
  </body>
</html>
